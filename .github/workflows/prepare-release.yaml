name: Prepare Release

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  prepare:
    uses: sweetrpg/github-actions/.github/workflows/go-prepare-release.yaml@master
    secrets: inherit
